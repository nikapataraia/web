import React from 'react';
import Map from './models/map';

interface Gamemapinterface {
  map_size : string;
}

const Gamemap: React.FC<Gamemapinterface> = ({ map_size }) => {
    
  return (
    <div>
    </div>
  );
};

export default Gamemap;