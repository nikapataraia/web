export default class map {
    height: number;
    width: number;
    map: number[];
    constructor(height: number, width: number, map: number[]) {
      this.height = height;
      this.width = width;
      this.map = map;
    }

  }